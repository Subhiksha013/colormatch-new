import { Sparkles } from 'lucide-react';
interface AnalysisLoaderProps { step: number; }
const labels = ['Analyzing your colours...', 'Detecting skin region...', 'Estimating undertone...', 'Creating your personal palette...', 'Your colours are ready!'];
export default function AnalysisLoader({ step }: AnalysisLoaderProps) { return <div className="text-center py-20 animate-fade-in"><div className="relative w-24 h-24 mx-auto mb-8"><div className="absolute inset-0 rounded-full border-2 border-neutral-200" /><div className="absolute inset-2 rounded-full border-2 border-neutral-900 border-t-transparent animate-spin" /><div className="absolute inset-0 flex items-center justify-center"><Sparkles className="w-8 h-8 text-neutral-800 animate-pulse" /></div></div><h2 className="font-display text-2xl font-bold text-neutral-900">{labels[Math.min(step, labels.length - 1)]}</h2><div className="flex justify-center gap-2 mt-6">{labels.slice(0, 4).map((_, i) => <div key={i} className={`h-1 rounded-full transition-all ${i <= step ? 'w-8 bg-neutral-900' : 'w-2 bg-neutral-200'}`} />)}</div></div>; }
